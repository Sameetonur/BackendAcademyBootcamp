using System.ComponentModel.Design.Serialization;
using System.Net.WebSockets;

namespace Proje_Sorular_Method;

class Program
{

    // Kendisine gönderlen int tipindeki sayı listesinde yer alan negatif sayıları geri döndüren bir method  hazırlayınız.
    // static List<int> GetNegativeNumbers(List<int> numbers)
    // {
    //     List<int> negativeNumbers = new List<int>();
    //     foreach (var number in numbers)
    //     {
    //         if (number < 0)
    //         {
    //             negativeNumbers.Add(number);
    //         }
    //     }
    //     return negativeNumbers;
    // }

    // static int TotalOfSquares(int a, int b)
    // {
    //     int islem = (a*a) + (b*b);
    //     return islem;
    // }

    static void Main(string[] args)
    {
        //    List<int> numbers=[ 5, 8, 9, 6, -8, -6, 2];
        //    List<int> resultNumbers= GetNegativeNumbers(numbers);
        //    foreach (var number in resultNumbers)
        //    {
        //     System.Console.WriteLine(number);
        //    }

        // Console.WriteLine(TotalOfSquares(3,4));// 5 ve 7 nin karelerini bulup toplayıp geri döndüren method

    }
}
